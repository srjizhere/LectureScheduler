export class ConstantData {
    static BASE_URL = "http://localhost:5000/api";
  
    static INSTRUCTORS = `${this.BASE_URL}/instructors`;
    static COURSES = `${this.BASE_URL}/courses`;
    static AUTH = `${this.BASE_URL}/auth`;
    // add more endpoints as needed
  }
  